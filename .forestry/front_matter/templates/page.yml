---
label: Page
hide_body: false
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
- name: featured_image
  type: file
  config:
    maxSize: 64
  label: Featured Image
- name: content
  type: blocks
  label: Content
  template_types:
  - text-section
  - cover-section
  config:
    min: 
    max: 
pages:
- content/vectorarrow.md
